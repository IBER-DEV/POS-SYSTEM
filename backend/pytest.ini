[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
addopts = --reuse-db -q
markers =
    slow: concurrency / transaction tests that spawn real threads
testpaths = apps tests
